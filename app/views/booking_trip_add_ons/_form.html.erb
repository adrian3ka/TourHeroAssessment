<%= form_with(model: booking_trip_add_on) do |form| %>
  <% if booking_trip_add_on.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(booking_trip_add_on.errors.count, "error") %> prohibited this booking_trip_add_on from being saved:</h2>

      <ul>
        <% booking_trip_add_on.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :booking_trip_id, style: "display: block" %>
    <%= form.text_field :booking_trip_id %>
  </div>

  <div>
    <%= form.label :add_on_id, style: "display: block" %>
    <%= form.text_field :add_on_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
