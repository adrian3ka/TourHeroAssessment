<h1>Editing booking trip add on</h1>

<%= render "form", booking_trip_add_on: @booking_trip_add_on %>

<br>

<div>
  <%= link_to "Show this booking trip add on", @booking_trip_add_on %> |
  <%= link_to "Back to booking trip add ons", booking_trip_add_ons_path %>
</div>
